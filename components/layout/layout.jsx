import Head from "next/head";

//Layout imports
import Footer from "./footer";
import Navbar from "./navbar";

export default function Layout({ children, title, pageTitle }) {
  const titleRendered = `CSVFormatter | ${title}`;
  return (
    <>
      <Head>
        <title>{titleRendered}</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="http://localhost:3000/img/favicon.ico" />
      </Head>

      <Navbar />

      <main className="container bg-dark border rounded main-content">
        <div className="text-center m-3">
          <h1>{pageTitle}</h1>
          <hr className="my-3" style={{ width: "80%", margin: "auto" }} />
        </div>
        {children}
      </main>

      <Footer />
    </>
  );
}

Layout.defaultProps = {
  title: "CSVFormatter",
  pageTitle: "Page",
};
